metadata:
  name: ${project.artifactId}-${project.version}
  labels:
    version: ${project.artifactId}
    app: ${project.artifactId}
spec:
  template:
    metadata:
      annotations:
        sidecar.istio.io/inject: "true"
    spec:
      containers:
        - resources:
            requests:
              cpu: "0.2"
              memory: 256Mi
            limits:
              cpu: "1.0"
              memory: 256Mi
          env:
            - name: "TICKETING_TICKET_PROCESSOR_SERVICE_URI_HOST"
              value: "istio-ingressgateway-istio-system.apps.cluster-jbuenosv-ee10.jbuenosv-ee10.example.opentlc.com"
            - name: "TICKETING_TICKET_PROCESSOR_SERVICE_URI_PORT"
              value: 80
