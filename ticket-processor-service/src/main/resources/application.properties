ticketing.sales.vitamine.on=${TICKETING_TICKETS_SERVICE_SALE_VITAMINE_ON}
ticketing.servicemesh.on=${TICKETING_TICKETS_SERVICE_MESH_ON}

ticketing.activemq.store.amq.enabled=${TICKETING_TICKETS_STORE_AMQ_ENABLED}
ticketing.activemq.broker.url=${TICKETING_TICKETS_BROKER_URI}
ticketing.activemq.broker.username=${TICKETING_TICKETS_BROKER_USERNAME}
ticketing.activemq.broker.password=${TICKETING_TICKETS_BROKER_PASSWORD}
ticketing.activemq.tickets.topic=${TICKETING_TICKETS_DESTINATION_NAME}

ticketing.event.version=${TICKETING_EVENT_VERSION}
ticketing.kafka.store.enabled=${TICKETING_TICKETS_STORE_KAFKA_ENABLED}

ticketing.stock.processor.service.uri=${TICKETING_STOCK_PROCESSOR_SERVICE_URI}

# prometheus monitoring
management.endpoint.metrics.enabled=${TICKETING_METRICS_ENABLED}
management.endpoints.web.exposure.include=${TICKETING_METRICS_WEB_EXPOSURE_INCLUDE}
management.endpoint.prometheus.enabled=${TICKETING_METRICS_PROMETHEUS_ENABLED}
management.metrics.export.prometheus.enabled=${TICKETING_METRICS_PROMETHEUS_EXPORT_ENABLED}
management.metrics.artifactid.name=${TICKETING_METRICS_ARTIFACTID_NAME}

info.app.name=${TICKETING_METRICS_ARTIFACTID_NAME}
info.app.version=${TICKETING_SERVICE_VERSION}
