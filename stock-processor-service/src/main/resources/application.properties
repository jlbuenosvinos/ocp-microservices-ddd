ticketing.activemq.broker.url=${TICKETING_TICKETS_BROKER_URI}
ticketing.activemq.broker.username=${TICKETING_TICKETS_BROKER_USERNAME}
ticketing.activemq.broker.password=${TICKETING_TICKETS_BROKER_PASSWORD}
ticketing.activemq.tickets.topic=${TICKETING_TICKETS_DESTINATION_NAME}

ticketing.stock.subscriber.client.id=${TICKETING_STOCK_SUBSCRIBER_CLIENT_ID}

stock.event.version=${STOCK_EVENT_VERSION}

stock.datagrid.server.url=${STOCK_DATAGRID_BROKER_URI}
stock.datagrid.server.port=${STOCK_DATAGRID_BROKER_URI_PORT}
stock.datagrid.username=${STOCK_DATAGRID_BROKER_USERNAME}
stock.datagrid.password=${STOCK_DATAGRID_BROKER_PASSWORD}
