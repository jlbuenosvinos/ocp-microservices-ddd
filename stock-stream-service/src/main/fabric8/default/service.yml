apiVersion: v1
kind: Service
metadata:
  name: ${project.artifactId}
  labels:
    version: v0
    app: ${project.artifactId}
    service: ${project.artifactId}
spec:
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: ${project.artifactId}
